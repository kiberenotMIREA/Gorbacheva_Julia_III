# Конфигурация проекта (`configs/`)

- `config.yaml` — все гиперпараметры (данные, модели, сервис)
- `.env.example` — шаблон переменных окружения (без секретов)

Использование: `python -m src.train --config configs/config.yaml`
